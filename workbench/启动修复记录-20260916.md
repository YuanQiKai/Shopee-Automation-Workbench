# 工作台访问修复记录

## 原因

- 2026-09-16 21:28 电脑重启；检查时 3010、8010、54329、7239、8239 均无本项目服务监听。
- 复现旧的双击入口时，Windows 返回 `.exe is not recognized`，未启动服务。旧批处理包含 UTF-8 中文脚本名且使用 LF 换行，存在 Windows 命令解释器兼容问题。

## 修复

- 批处理改成 ASCII 内容、Windows CRLF 换行；通过英文脚本名启动，同时保留中文入口。
- 项目根目录增加 `打开选品工作台.cmd`。
- 启动前检查构建和运行环境，按顺序启动现有数据库、Temporal、接口、页面及任务执行器。
- 页面打开前验证真实数据库健康、前端转发和产品清单接口。
- 增加启动锁，及时保存进程记录；使用进程创建时间和程序路径判断任务执行器，替换 Windows 下不安全的 `os.kill(pid, 0)`。
- 增加启动日志；保留失败退出码。

## 验证

- 从全部服务停止的状态，通过根目录新入口启动成功，退出码 0。
- 再次通过原中文入口启动成功，退出码 0，进程记录完全一致，没有重复启动。
- 重复启动前后，候选、货源、经营设置的 JSON 哈希一致。
- 浏览器实际显示工作台；22 个候选、2 家供应商、38 条 SKU 记录可读。
- 在页面点击重算后，Temporal 任务完成，检查了 22 个候选。
- Excel 导出成功，包含 10 个工作表。
- 6 项启动回归测试通过：批处理编码与换行、失效进程编号、进程身份、无损存活检测、接口识别、失败进程及时退出。

本地访问地址：http://127.0.0.1:3010。电脑关机后服务会停止；重新开机后双击启动入口即可。未设置开机自动运行。
